import { Box, Container } from "@mui/material";

const Filter = () => {
  return (
    <Box
      sx={{
        marginTop: "500px",
      }}
    >
      <h1>Menu_Filter</h1>
    </Box>
  );
};

export default Filter;
